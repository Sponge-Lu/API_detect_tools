## 问题确认
- 当前“控制台导入”模式下仍显示顶部的自动识别步骤指示器（1→4），造成流程混杂。
- 目标是让“控制台导入”成为完全独立的入口：仅显示导入所需的控件与说明，不出现自动识别步骤提示或交互。

## 目标体验
- 在“控制台导入”模式下：
  - 显示专用的 URL 输入（带粘贴与自动补全，严格校验与错误提示）。
  - 显示推荐页面链接、复制/打开目标地址、复制控制台脚本、导入文本框与“导入并保存”。
  - 隐藏自动识别步骤指示器与“下一步：浏览器登录”等自动化控件。

## 代码改动
- 条件渲染步骤指示器：将 `步骤指示器` 块改为仅在 `mode==='auto'` 时渲染（位置：src/renderer/components/SiteEditor.tsx:722）。
- 保持导入区块独立：保留现有 `mode==='import'` 的导入卡片（位置：src/renderer/components/SiteEditor.tsx:828），其内含：
  - 目标站点 URL 输入与错误提示（已接入：`handleUrlChange`/`handleAutoCompleteUrl`/`handlePasteUrl`）。
- 推荐页面 URL 计算统一：将 `getTargetConsoleUrl` 改为使用 `normalizeUrl(url)` 与 `isValidUrlStrict` 校验，返回 `origin+'/console/token'` 或空字符串（位置：src/renderer/components/SiteEditor.tsx:482）。
- 复制/打开按钮的可用性：
  - 在 `handleCopyTargetUrl`/`handleOpenTargetUrl` 内，先对 `url` 执行 `normalizeUrl` + `isValidUrlStrict`，无效则提示并禁用。
- 函数级注释（中文）：为以上改动的函数/渲染块补充中文注释，说明职责与使用方式。

## 验证方案
- 切换到“控制台导入”模式：确认不再显示步骤指示器；URL输入/错误提示/粘贴/自动补全正常。
- 输入非法 URL：看到红色错误提示；复制/打开按钮提示不可用。
- 输入合法 URL：推荐链接与复制/打开按钮工作正常；脚本复制后在推荐页控制台执行输出 JSON；粘贴到文本框并“导入并保存”成功。

请确认以上计划，确认后我将按步骤修改并自测。