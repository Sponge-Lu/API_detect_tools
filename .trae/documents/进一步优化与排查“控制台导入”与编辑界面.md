## 隐患与问题点
- 未使用函数与告警：
  - `getConsoleScript`、`getSafeConsoleScript` 未被引用，持续产生告警，增加维护负担。
  - Lucide 的 `Chrome` 图标提示“已弃用”，建议替换为更稳定的图标（如 `Globe` 或 `ExternalLink`）。
- 交互一致性：
  - 当 URL 无效时，复制/打开按钮仍可点击，仅通过提示阻止；可改为禁用按钮提升体验。
- 最小化脚本的细节：
  - `site_name` 在无法读取状态时为 `""`；建议回退为 `hostname`，保证导入后显示友好名称。
  - 输出时已清理反引号，但可以统一再清理潜在空白/尾部斜杠。
- 主题与样式一致性：
  - 说明卡片已改为主题变量，但其他提示区（错误/成功提示）仍部分使用固定色；可统一到主题变量，保证暗/亮主题一致表现。

## 计划优化项
1. 清理未使用代码
   - 删除 `getConsoleScript` 与 `getSafeConsoleScript`，移除相关告警。
2. 改进最小化脚本（函数级中文注释）
   - `getMinimalConsoleScript`：`site_name` 默认使用 `hostname`；`site_url` 统一 `trim()`；完善错误日志中文描述。
3. 按状态禁用按钮
   - 当 `getTargetConsoleUrl()` 为空时，禁用“复制目标地址”“打开登录页”按钮，同时保留现有提示。
4. 替换弃用图标
   - 将 `Chrome` 图标替换为 `Globe` 或 `ExternalLink`，减少“已弃用”提示。
5. 统一主题色
   - 将错误/成功提示背景与边框改为 `light/dark` 主题变量，提升夜间模式一致性。

## 验证方案
- 启动开发环境，检查 VS Code 诊断是否清除未使用函数告警与图标弃用提示。
- 在“控制台导入”模式：
  - 无效 URL：按钮禁用，不可点击；推荐链接非可点文本。
  - 有效 URL：复制/打开按钮可用，链接正常。
- 在目标站点推荐页执行脚本：
  - 无 `/api/user/token` 时仍可从 `/api/token/?...` 读取首个 `key`；输出 JSON 的 `site_name` 为域名友好值。
- 暗/亮主题切换，检查提示卡片颜色是否统一一致。

如你确认以上优化项，我将直接实施并自测，所有函数都会添加中文函数级注释。